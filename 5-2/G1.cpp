#include <cstdio>

char str[200001];
int main() {
	int tc;
	scanf("%d", &tc);
	while(tc--) {
		int n, l, r;
		scanf("%d%d%d", &n, &l, &r);
		scanf("\n%s", str);
		int st = 0, ed = n / l;
		while(st < ed) {
			int md = (st+ed) / 2;
			for(int i = 0; i < )
		}
	}
}
