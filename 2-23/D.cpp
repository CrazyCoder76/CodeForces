#include <bits/stdc++.h>
using namespace std;

int a[300001];
int l[300001];
int sum[300001];
int r[300001];

void func(int* val, int* ,int n)
int main() {
	int tc;
	cin >> tc;
	while(tc--) {
		int n, k;
		cin >> n;
		for(int i = 0; i < n; i++) {
			cin >> a[i];
			sum[i] = a[i];
		}
		
	}
}
